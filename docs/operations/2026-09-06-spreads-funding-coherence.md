# Keep live Spreads funding explanations and calculator coherent

Production ICX WhiteBIT Futures -> Kraken Futures changed funding sign around 06:00 UTC. The stream updated the headline to +3.225%, but the page still said pay and printed older leg rates (+0.0100% / -0.0805%); Net edge retained -1.962087%. Native Kraken PF_ICXUSD at 06:03:52 confirmed the new positive rate. The numeric stream was not itself evidence of a sign-calculation bug: the dependent text and calculator state were stale.

The local fix reads the shared current funding snapshot for every board stream, rather than only funding_only streams. Each exact route's current projection, leg rates, cadence and direction label derive from that one snapshot. Expiry clears current funding instead of reusing older headline inputs. Spread/depth calculation and public visible-route filtering remain unchanged. Loading the existing funding snapshot is once per shared stream build, not once per leg.

Expanded Spreads rows expose live hooks for rates, cadence, basis and direction. An unavailable current headline retains its live hook so it can recover later. Group explanations update only where the group explicitly owns a live funding headline; historical labels are not rewritten. The stream updates each exact row's Net edge dataset and emits an exact-route event so an already-open calculator also recalculates. Only current_funding_24h_pct changes; settled windows, matched-size quote, route identity and user inputs remain intact. A different route's event cannot update that calculator.

Tests exercise ordinary and funding-only streams, absent snapshots, actual rendered hooks, live DOM event updates, sign reversal, expiry and zero. A combined stream/calculator JavaScript harness verifies already-open calculator updates and preserves exact settled seven-day amounts. Six cases fail against the original functions/JavaScript; five unaffected cases pass (0.98s, exit1). Focused suite27passed3.32s, exit0. Two older stream tests were adjusted for the coherent four-field payload and unavailable funding because their quote-only fixtures contain no funding-leg evidence; visible-key/price assertions remain. Ruff no new516against unchanged517baseline, exit0.

Full suite:2694passed174.13s, actual exit0. No new Ruff findings516against unchanged517baseline, exit0. Tracked test-generated data restored; no product source edits after gates. Live remains f047ccf; candidate includes earlier6ab7440 memory changes and7b7f904 cache revalidation. No deployment queued. Sole observer499780 freshlyactive06:11:00; preserve until07:12:50. Further normal RAM/CPU/freshness and actual browser verification are required after guarded release.
