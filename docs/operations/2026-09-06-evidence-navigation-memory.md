# Keep websocket paused through history funding publication

The 05:28:05 UTC production sample shows funding-navigation and websocket workers together at the current observation's highest sampled collector anonymous memory (3,204,747,264 bytes). This does not identify which navigation caller launched that particular child. Code inspection separately proves that MarketEvidenceLoop resumed websocket immediately after its evidence child, before its funding-navigation child, despite retaining the collector heavy lock.

The local candidate keeps both children within the existing pause/finally-resume scope. Bulk quote collection, funding calculations, publication cadence and source membership are unchanged. Failed evidence does not publish funding navigation; evidence and navigation exceptions still restore websocket and release both locks. No additional pause is introduced for callers without a refresh loop. The obsolete comment about avoiding a different materialized-view build was replaced with the exact funding-navigation memory rationale.

Four regression cases invoke the real _sweep_once path and assert worker exclusion, call order and lock release: success, nonzero evidence result, raised evidence error and raised navigation error. The original method fails the success and navigation-error cases at the exclusion assertion. Targeted suite: 57 passed in 2.77 seconds, exit 0. Original-source run: two failed/two passed in 0.35 seconds, exit 1. Evidence: output/stability-20260906/evidence-navigation-{targeted,original}.txt.

Review limits: the pause now lasts through the bounded funding-navigation call (configured timeout defaults to 900 seconds). Bulk quotes remain active, but fast-lane freshness and overall peak memory still need production observation after a guarded release. Other callers, notably FundingCatalogPublisher and _schedule_funding_navigation, are not fixed by this change; review their lock ownership and refresh-loop wiring separately. No all-callers exclusion or whole-service memory-saving claim is made.

Live remains f047ccf. This candidate also contains the separately tested same-key cache candidate 7b7f904. No deployment is queued; preserve the sole production observer 499780 through its expected 07:12:50 UTC end. Final full suite: 2,686 passed in 154.46 seconds, actual exit 0. Ruff: no new findings, 516 known against the unchanged 517 baseline, actual exit 0. Logs: evidence-navigation-full.txt and evidence-navigation-ruff.txt. Tracked test-generated data was restored after the suite; no product source changed after these gates. Caps and recurring automation remain unchanged/PAUSED.
