# Stream index encoding instead of retaining a second 512 MB copy

Later ordinary observation invalidated the earlier lower collector peaks: at06:25:11 collector anonymous memory reached3,879,731,200bytes with websocket absent. The index worker used2,254,924KiB RSS, discovery643,724KiB and supervisor504,084KiB; bulk/fast workers remained active. Host MemAvailable552,960KiB. The current published index has208,911rows and512,479,951bytes. Existing writer encoded the whole sorted JSON object before writing it, retaining a further large byte string beside decoded routes. Index+discovery overlap is intentional for fresh publication; pausing discovery for its whole scan would sacrifice freshness.

The default write_live_route_index path now serializes one sorted route key/value at a time to a buffered file, hashes exactly those bytes, fsyncs it and only then atomically replaces the pointer. Metadata includes the resulting size/checksum. The supplied-encoded path remains supported for callers which already own an encoding. No routes, keys, economics, freshness or membership are changed. A failed row encoding never changes the old pointer; an unreferenced partial file is handled by existing later obsolete-file cleanup.

Five writer cases include empty and Unicode/nested roundtrip, whole-dictionary encoding rejection through the actual publication method, mid-stream failure retaining the previous readable pointer, and supplied encoding.16focusedtests passed1.61s. Original writer:twofail/threepass0.11s exit1. Ruffno new516against unchanged517baseline,exit0. Full suite: 2702 passed in 140.73s (0:02:20), actualexit0. Tracked test-generated data restored; no source changes after gates.

Separate synthetic208,911-route Mac profiles emitted IDENTICAL512,997,577bytes/SHA256b5a0d7e55ae0cd20a13572768a9b3e875a25c9d18288fe4cbbc46485a13b331a. Incremental traced peak:original541,884,881bytes versusstreamed3,521,252bytes (538,363,629bytes lower). Traced time1.197s versus3.259s; this is an encoding/allocation comparison, not production RSS/CPU or full-build peak saving. Setup excluded; temporary output removed; both processes terminalexit0. Actual production headroom must be observed after deployment, so do not subtract the synthetic saving from the real peak to choose caps.

Evidence output/stability-20260906/combined-collector-peak-continued.json, index-writer-profile-{original,streamed}.json, profile_index_writer.py, index-writer-{targeted,original,ruff,full}.txt. Livef047ccf; localcandidate also includesfc8e2f4installcoherence,2746e4efundingUI,6ab7440navigationmemory and7b7f904cache. No deployment/waiter/capchanges. Soleobserver499780 until07:12:50; normalbackup532835 stillrunning atlastcheck06:25:39, notterminalsuccess. AutomationPAUSED.
