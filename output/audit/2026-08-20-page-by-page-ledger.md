# SpreadBoard page-by-page production audit — 2026-08-20

Production audited at `https://spreadarbitrage.ink` with a temporary, normally
authenticated Research Pro account. Screenshots are under
`output/playwright/2026-08-20-page-audit/`.

| Page / area | Issues found | Fixed | Verified how | Left open |
| --- | --- | --- | --- | --- |
| Deployment preflight | `.com` is still delegated to Afternic parking; it is not the canonical `.ink` production host. | No infrastructure mutation: current authoritative nameservers are outside the Namecheap `.ink` zone. | Local/app/collector SHA-256 equality for `server.py`, `portfolio.py`, `fair_price.py`, and `fast_quotes.py`; fresh `dig`, `curl`, and browser navigation. | Owner-controlled GoDaddy/Afternic DNS recovery if `.com` is still wanted. |
| Funding (`2406456`) | No regression found in the entry-basis fix. | Already deployed. | Signed-in 1440px production render; zero `refreshing`/`unavailable` text and every visible Entry Basis cell had a numeric percentage. | Full dark/mobile/control pass remains. |
| Rankings (`6029032`) | No blank top-row spread regression. The page uses `refreshing both legs` as a status while preserving the known spread. | Already deployed. | First eight production rows all had numeric Spread Now values. | Full dark/mobile/control pass remains; wording/status semantics require review with the broader Markets audit. |
| Fair price (`b3d7a09`) | No low-turnover row leaked through. | Already deployed. | 26 production cards, minimum displayed 24h turnover `$27,433`, zero rows below `$25,000`; deployed `fair_price.py` hash matches local. | Full dark/mobile/control pass remains. |
| Charts (`33f5410`) | No quote deletion regression. | Already deployed. | Built BTW Mexc Spot `BTW/USDC` to Aster Futures `BTW/USDT:USDT`; live sample returned `status=ok`, inserted one observation, and populated the chart while the unused Mexc reverse `bid_vwap` was `null`. | Full chart-control/theme/mobile pass remains. |
| Portfolio (`aa288c5`, `7557724`) | Correct partial totals were contradicted by stale copy saying Total PnL was withheld. | Local failing-test-first fix changes the note to say the shown partial Total PnL excludes settled funding. | Temporary priceable BTW plus unpriceable audit fixture showed `Total PnL -1.11 USD` with one position excluded, partial return, separated movement/fees/funding, and successful UI deletion. Targeted suite: 121 passed. Fixtures removed. | Deploy/reverify the copy fix after the complete audit gate. Full edit/close/alerts/settings/mobile pass remains. |
| Markets — values and route evidence §6.2 | The exact-size calculator accepted top-book fallback as target-depth proof. Pro Table discarded a known current funding projection whenever settled 24h history was not ready: named GUA `Ourbit → Gate` showed `—` while the backing row carried `+0.6108% / 24h`. Exact economic duplicates also appeared under canonical and `CUSTOM:` keys. | Exact-size quote returns `exact_route_target_depth_unavailable` unless directional target VWAP is proven. Spot-only carry stays not-applicable. Pro Table now prefers settled 24h but falls back to a known current projection and labels the source `24h at current rate`. Exact-leg catalogue rows are deduplicated without discarding richer settled/provider evidence. | Named production traces: WKC exact $1,000 quote returned a real matched value; ZIL/ALIGN/LUNC thin targets returned honest unavailable errors; GUA `Ourbit → Gate` changed from a dash to a labelled current projection. Final authenticated DOM/API reconciliation covered 3/3 current GUA routes and found 0 known-number dashes across matched edge, Funding 24h, 1d, 7d, 30d and depth. | A dash remains intentional where the route genuinely lacks a matched ladder, settled window, metadata or rail proof; the adjacent top-book/unverified/history label states why. |
| Markets — DEX source truth and filtered summaries §6.2 | Both DEX lanes used a generic no-match state even though the current cycle had zero verified DEX quotes because OKX rejected provider access on chains 1, 56, 137, 42161 and 8453. A zero-row DEX tab also inherited the global CEX largest-edge KPI, and filtered views did not identify the side rankings as global. | DEX-Spot, DEX-Futures and Funding DEX now name the provider rejection and explicitly say it is not evidence no route exists. Filtered KPIs come only from their represented result set; market-wide sidebars are labelled `Market-wide` and `Across all current lanes`. | Production source health: 2,241 discovered DEX identities, 0 current verified DEX quote rows, both top-25 readiness gates false, provider rejection visible in desktop/mobile DEX lanes, DEX zero-row largest edge `—`, console clean. | External blocker: restore an entitled/region-compatible OKX DEX quote provider; no route or readiness gate was weakened. |
| Markets — refresh and controls §6.2 | Structural in-place refresh inserted inert DOMParser controls. Preset save falsely errored after `await`; preset delete and Copy View then failed. Preserving the form fixed listeners but froze a cold shell's empty Exchanges dropdown forever. A query-only stale structural generation later retained old expanded-route spreads. | Live interactive islands retain identity and unsaved values; server-derived exchange options merge into the preserved select after warm-up; route/KPI/count evidence still refreshes. Preset save/delete mutate chips without navigation and Copy View delegates from the stable document. The stale-generation cache and rebuild path were removed: a snapshot/signature change now produces the current generation or an honest warming response, never an older query-only structure. | Forced the exact production refresh path: root replaced, live nodes retained identity, unsaved `GUA UNSAVED` survived, Exchanges recovered 1 → 21 choices including Gate, time origin unchanged; preset delete/save/delete `200/201/200`; Copy View `Link copied`; refreshed ZIL opened Net Edge; 0 console errors/warnings. Source and regression tests confirm `_MARKET_STALE_CACHE`, `allow_stale` and stale rebuild hooks are absent. | Current-generation correctness is closed. Cold ad-hoc generations can still honestly report `warming`; the export UX improvement is tracked separately. |
| Markets — live expanded-route updates §6.2 | A correct SSE leader tick matched both the outer `<details>` group and its best child route key; the updater then traversed the entire group and overwrote every expanded child with the leader's spread. Named OPENAI rows visibly collapsed to the same `+3.27%` despite exact current API values near `+0.13%`–`+0.19%`. Expanded routes outside the resident WebSocket set could also retain an older structural value. | Group ticks are scoped to their direct `<summary>` while every child row is updated only by its own route-key match. Exact current fast-quote deltas now cover routes outside the bounded resident-book set, keyed by token, venue, type and symbol and freshness-limited to the unchanged live boundary. | Failing-test-first DOM-scope regression plus exact fast-route tests; production OPENAI had 14 expanded rows with 12 distinct values rather than one leader value. Eight rows present in the same live event reconciled to the displayed DOM with zero differences above rounding tolerance; no console errors. | Six expanded rows were intentionally absent from that single live event and retained their timestamped structural value; the stream did not fabricate or erase them. |
| Markets — filters, export, pagination and layout §6.2 | The new `24h at current rate` label reintroduced a 94px desktop overflow, hiding the right-side actions. The earlier two-column layout also pushed 7d/30d beyond the 1440px viewport. | Pro Table owns the full page width; evidence labels and actions wrap inside their cells; market-wide side panels move below. Every basic/advanced filter retains and submits its value; native invalid step values are rejected; Reset and both pagination directions work. | Authenticated production: full advanced submission covered token, exchange, edge, funding, sort, direction, quote, volume, cap, FDV, age, persistence and funding-only; Reset returned `/markets`; pagination reached `26-50 of 997` and back; the 500-asset JSON endpoint completed with 5,570 routes. At 1440px wrap `1342/1342`, all columns/actions fit; at 375px document and table `375/319`, no horizontal overflow and 1d/7d/30d cards present. Final light/dark desktop/mobile screenshots saved; full suite `1,450 passed`; Ruff ratchet `0` new (`532` known). | First uncached 500-asset JSON request reports `warming` until its generation finishes; the subsequent export is valid. Replace this raw cold state with an explicit preparing/export flow. |
| Arbitrage legacy alias `/arbitrage` | Shares the Markets renderer and therefore inherited both the stale-generation and group-leader fan-out defects above. | The same current-generation and exact DOM-scoping fixes cover the alias. Controls intentionally submit to canonical `/markets`; the alias URL itself remains supported. | Authenticated production at `/arbitrage?kind=FUTURES`: 25 groups, 246 expanded route rows, zero displayed dashes, expand/collapse works, filter form action is `/markets`, body/main fit `1440/1440` and `375/375`, light/dark desktop/mobile screenshots saved, console clean. | No alias-specific defect remains; keep the redirect/canonicalization decision explicit during later navigation cleanup. |
| Future ML readiness | Data volume, route coverage, class balance, leakage, and purged time split pass; exact-cost completeness and time span do not. | No model trained or activated; shadow deterministic fallback preserved. | Fresh production worker through `/app/.venv/bin/python`: 23,202 v5 observations, 15,924/5,000 labeled 24h outcomes, 1,733/100 routes, 6.21/30 days, 0.0/0.8 exact-cost completeness, funding minority 0.1064, spread minority 0.2748, leakage clean, valid 24h-embargoed split, no candidate, activation false. | Preserve collection for at least 30 labeled days and collect opt-in exact lifecycle-cost evidence to reach 80%; candidate evaluation remains correctly absent until data readiness is valid. |

## Named commit re-verification

- `2406456`: verified in signed-in production Funding.
- `aa288c5`: verified with two disposable production portfolio fixtures.
- `7557724`: verified with partial funding/PnL fields; discovered and fixed the contradictory note locally.
- `6029032`: verified in the first eight signed-in production ranking rows.
- `b3d7a09`: verified on 26 current Fair Price cards.
- `33f5410`: verified with the exact BTW thin reverse-book condition in the live chart pipeline.
