# SpreadBoard page-by-page production audit — 2026-08-20

Production audited at `https://spreadarbitrage.ink` with a temporary, normally
authenticated Research Pro account. Screenshots are under
`output/playwright/2026-08-20-page-audit/` and
`output/playwright/2026-08-21-page-audit/`.

| Page / area | Issues found | Fixed | Verified how | Left open |
| --- | --- | --- | --- | --- |
| Deployment preflight | `.com` is still delegated to Afternic parking; it is not the canonical `.ink` production host. More seriously, the current `.deployed_revision` marker masked three stale production modules: `fair_price.py`, core `fast_quotes.py`, and `venue_funding_history.py`. | Synchronized all three modules to the persisted host, app and collector and restarted both services healthy. Added a read-only whole-package verifier with SSH/container input validation and duplicate-manifest rejection. | `.venv/bin/python scripts/verify_production_source_sync.py` now proves all 51 local `spreadboard/**/*.py` modules match the persisted host, `app-app-1` and `app-collector-1`, with no changed, missing or unexpected files. Full suite `1,475 passed`; Ruff ratchet `0` new (`532` known). | Owner-controlled GoDaddy/Afternic DNS recovery if `.com` is still wanted. The revision marker remains metadata, never sufficient deployment proof by itself. |
| Funding (`2406456`) | No regression found in the entry-basis fix. | Already deployed. | Signed-in 1440px production render; zero `refreshing`/`unavailable` text and every visible Entry Basis cell had a numeric percentage. | Full dark/mobile/control pass remains. |
| Rankings (`6029032`) | No blank top-row spread regression. The page uses `refreshing both legs` as a status while preserving the known spread. | Already deployed. | First eight production rows all had numeric Spread Now values. | Full dark/mobile/control pass remains; wording/status semantics require review with the broader Markets audit. |
| Fair price (`b3d7a09`) | The production module had drifted behind the marker and the low-turnover regression was live again. Named Kraken Futures ANTHROPICX showed `-32.06%`; its current ticker had `baseVolume=0.02`, `last=2099.95`, no `quoteVolume`, and `info.volumeQuote=41.999`, far below the `$25,000` floor. | Deployed the committed `_turnover_usd` fallback/reject logic to persisted host, app and collector and restarted both services. | Production now renders 34 Fair Price cards, zero unknown-volume cards, zero cards below `$25,000`, and no ANTHROPICX. Local `tests/test_fair_price.py` passed and the complete package verifier confirms the deployed module matches local. | Full dark/mobile/control pass remains. Unknown turnover stays rejected deliberately because this page promises a liquidity-screened research set; `/top` remains the labelled unproven surface. |
| Charts (`33f5410`) | The core `fast_quotes.py` module had also drifted behind the marker, so production still required both bid/ask VWAPs and non-null depth despite commit `33f5410`. | Deployed the current partial-side quote logic to persisted host, app and collector and restarted both services. | A disposable production-process fixture recreated the exact thin reverse-book case: `bid_vwap=null`, `ask_vwap=0.4202655`, and the required leg remained. The signed-in live BTW Mexc Spot → Aster Futures chart now reports `Streaming · exact books`, numeric In, `$500 VWAP`, and Out values with no first-observation empty state, zero console warnings/errors and 200 history/catalog calls. Whole-package verification passes 51/51. | Full chart-control/theme/mobile pass remains. The history footer currently says `0 observations · loading window history` while a live stream sample is visible; audit that count/state during the full Charts pass rather than relabelling it without tracing persistence. |
| Portfolio (`aa288c5`, `7557724`) | Correct partial totals were contradicted by stale copy saying Total PnL was withheld. | Local failing-test-first fix changes the note to say the shown partial Total PnL excludes settled funding. | Temporary priceable BTW plus unpriceable audit fixture showed `Total PnL -1.11 USD` with one position excluded, partial return, separated movement/fees/funding, and successful UI deletion. Targeted suite: 121 passed. Fixtures removed. | Deploy/reverify the copy fix after the complete audit gate. Full edit/close/alerts/settings/mobile pass remains. |
| Markets — values and route evidence §6.2 | The exact-size calculator accepted top-book fallback as target-depth proof. Pro Table discarded a known current funding projection whenever settled 24h history was not ready: named GUA `Ourbit → Gate` showed `—` while the backing row carried `+0.6108% / 24h`. Exact economic duplicates also appeared under canonical and `CUSTOM:` keys. | Exact-size quote returns `exact_route_target_depth_unavailable` unless directional target VWAP is proven. Spot-only carry stays not-applicable. Pro Table now prefers settled 24h but falls back to a known current projection and labels the source `24h at current rate`. Exact-leg catalogue rows are deduplicated without discarding richer settled/provider evidence. | Named production traces: WKC exact $1,000 quote returned a real matched value; ZIL/ALIGN/LUNC thin targets returned honest unavailable errors; GUA `Ourbit → Gate` changed from a dash to a labelled current projection. Final authenticated DOM/API reconciliation covered 3/3 current GUA routes and found 0 known-number dashes across matched edge, Funding 24h, 1d, 7d, 30d and depth. | A dash remains intentional where the route genuinely lacks a matched ladder, settled window, metadata or rail proof; the adjacent top-book/unverified/history label states why. |
| Markets — DEX source truth and filtered summaries §6.2 | Both DEX lanes used a generic no-match state even though the current cycle had zero verified DEX quotes because OKX rejected provider access on chains 1, 56, 137, 42161 and 8453. A zero-row DEX tab also inherited the global CEX largest-edge KPI, and filtered views did not identify the side rankings as global. | DEX-Spot, DEX-Futures and Funding DEX now name the provider rejection and explicitly say it is not evidence no route exists. Filtered KPIs come only from their represented result set; market-wide sidebars are labelled `Market-wide` and `Across all current lanes`. | Production source health: 2,241 discovered DEX identities, 0 current verified DEX quote rows, both top-25 readiness gates false, provider rejection visible in desktop/mobile DEX lanes, DEX zero-row largest edge `—`, console clean. | External blocker: restore an entitled/region-compatible OKX DEX quote provider; no route or readiness gate was weakened. |
| Markets — refresh and controls §6.2 | Structural in-place refresh inserted inert DOMParser controls. Preset save falsely errored after `await`; preset delete and Copy View then failed. Preserving the form fixed listeners but froze a cold shell's empty Exchanges dropdown forever. A query-only stale structural generation later retained old expanded-route spreads. | Live interactive islands retain identity and unsaved values; server-derived exchange options merge into the preserved select after warm-up; route/KPI/count evidence still refreshes. Preset save/delete mutate chips without navigation and Copy View delegates from the stable document. The stale-generation cache and rebuild path were removed: a snapshot/signature change now produces the current generation or an honest warming response, never an older query-only structure. | Forced the exact production refresh path: root replaced, live nodes retained identity, unsaved `GUA UNSAVED` survived, Exchanges recovered 1 → 21 choices including Gate, time origin unchanged; preset delete/save/delete `200/201/200`; Copy View `Link copied`; refreshed ZIL opened Net Edge; 0 console errors/warnings. Source and regression tests confirm `_MARKET_STALE_CACHE`, `allow_stale` and stale rebuild hooks are absent. | Current-generation correctness is closed. Cold ad-hoc generations can still honestly report `warming`; the export UX improvement is tracked separately. |
| Markets — live expanded-route updates §6.2 | A correct SSE leader tick matched both the outer `<details>` group and its best child route key; the updater then traversed the entire group and overwrote every expanded child with the leader's spread. Named OPENAI rows visibly collapsed to the same `+3.27%` despite exact current API values near `+0.13%`–`+0.19%`. Expanded routes outside the resident WebSocket set could also retain an older structural value. | Group ticks are scoped to their direct `<summary>` while every child row is updated only by its own route-key match. Exact current fast-quote deltas now cover routes outside the bounded resident-book set, keyed by token, venue, type and symbol and freshness-limited to the unchanged live boundary. | Failing-test-first DOM-scope regression plus exact fast-route tests; production OPENAI had 14 expanded rows with 12 distinct values rather than one leader value. Eight rows present in the same live event reconciled to the displayed DOM with zero differences above rounding tolerance; no console errors. | Six expanded rows were intentionally absent from that single live event and retained their timestamped structural value; the stream did not fabricate or erase them. |
| Markets — filters, export, pagination and layout §6.2 | The new `24h at current rate` label reintroduced a 94px desktop overflow, hiding the right-side actions. The earlier two-column layout also pushed 7d/30d beyond the 1440px viewport. | Pro Table owns the full page width; evidence labels and actions wrap inside their cells; market-wide side panels move below. Every basic/advanced filter retains and submits its value; native invalid step values are rejected; Reset and both pagination directions work. | Authenticated production: full advanced submission covered token, exchange, edge, funding, sort, direction, quote, volume, cap, FDV, age, persistence and funding-only; Reset returned `/markets`; pagination reached `26-50 of 997` and back; the 500-asset JSON endpoint completed with 5,570 routes. At 1440px wrap `1342/1342`, all columns/actions fit; at 375px document and table `375/319`, no horizontal overflow and 1d/7d/30d cards present. Final light/dark desktop/mobile screenshots saved; full suite `1,450 passed`; Ruff ratchet `0` new (`532` known). | First uncached 500-asset JSON request reports `warming` until its generation finishes; the subsequent export is valid. Replace this raw cold state with an explicit preparing/export flow. |
| Arbitrage legacy alias `/arbitrage` | Shares the Markets renderer and therefore inherited both the stale-generation and group-leader fan-out defects above. | The same current-generation and exact DOM-scoping fixes cover the alias. Controls intentionally submit to canonical `/markets`; the alias URL itself remains supported. | Authenticated production at `/arbitrage?kind=FUTURES`: 25 groups, 246 expanded route rows, zero displayed dashes, expand/collapse works, filter form action is `/markets`, body/main fit `1440/1440` and `375/375`, light/dark desktop/mobile screenshots saved, console clean. | No alias-specific defect remains; keep the redirect/canonicalization decision explicit during later navigation cleanup. |
| Playbook `/playbook` | The cards and answer boxes used hard-coded light surfaces while the dark editorial theme switched their text to pale values, making the page effectively unreadable in dark mode. The single displayed dash was investigated and is not withheld data: the topic brief and its age are both absent. | Failing-test-first CSS fix makes cards, answers, labels, steps and examples consume the shared terminal surface, line, text and muted tokens in both themes. | Authenticated production screenshots cover desktop/mobile and light/dark after deployment. At 375px, document and main are exactly `375/375`; dark contrast is `16.37:1` for headings, `7.11:1` for descriptive text and `15.95:1` for answers. `/api/playbook` returned `200`, seven zero-count/zero-example cards, Telegram `stale` at about `121.1h`, and a null topic brief/age matching the DOM. Pause/Resume, theme toggle, explicit Community click/back and every other unique content destination were exercised; all ten destinations returned `200`. Console has zero errors/warnings and the page request log has no failed call. Full suite `1,451 passed`; Ruff ratchet `0` new (`532` known). | The underlying community evidence is stale and the topic brief is missing. The page now states that truth; no synthetic examples or freshness claim was added. Restoring always-on source collection is a separate data/infrastructure task, not a rendering fix, and no Telegram message was sent. |
| Profile `/profile` | An empty book displayed dashes for known zero cash totals. Every account mutation still reloaded the whole document; overlapping quote refreshes could visually restore an older panel. Disabled Pushover claimed active-device success, its test and Web Push enable left buttons disabled after an async yield, Web Push queued a misleading test without a subscription, and a mistyped delete confirmation exposed an internal error code. During the repair, an incorrect `.public_dict()` call on the already-serialized account-settings result caused a production `500`; it was caught in live testing and immediately corrected with an endpoint-level regression test. | Empty cash/deployment totals now render exact zero while denominator-only returns remain unknown. Server-rendered account panels refresh in place with a newest-request guard, stable delegated controls and truthful refresh-failure status. Settings, exchange/Telegram fragments, position add/edit/alert/delete, and notification reads no longer reload. Pushover/Web Push states and errors are truthful, browser tests require an owned subscription, and delete confirmation is human-readable. | A disposable GUA Mexc Spot → Gate Futures journal entry exercised 12 catalogue legs, 50 pairs including OKX DEX 56, create, exact mark PnL/funding-unknown copy, edit, two alert rules and one worker trigger, close-form validation, chart link and UI deletion. `performance.timeOrigin` and a sentinel survived each mutation. Exact DB proof after deletion: position, rules, test notification, Telegram token/link and prior browser-test artifact all zero. Empty `/api/portfolio` returned zero positions and exact zero cash/deployment totals. Production desktop/mobile light/dark and settings screenshots fit `1440/1440` and `375/375`; final console and request log are clean. Local/host/app/collector hashes match; full suite `1,460 passed`; Ruff `0` new (`532` known). | Successful Pushover, real browser-push delivery, exchange connect/disconnect and completed-position research contribution were not mutated without the owner's credentials/provider permission or genuine lifecycle evidence. `/profile` and `/account` share the same renderer; `/account` still needs its explicit route/alias pass. No trade or Telegram message occurred. |
| Intel `/intel` | The customer page ignored the always-on subscription webhook's anonymous token/view events and read only a Mac-dependent Telethon bridge file that was empty. It therefore advertised the wrong private bot, linked `@SpreadArbitrageBot`, and could retain a new event behind a 15-minute cache. On mobile the long subscription-bot username forced the page to 470px and the fixed refresh pill covered the empty-state card. | Intel now consumes only the server-side `anonymous_bot_attention.v1` feed already written by the subscription webhook; no identity or raw text is added. The cache key includes the event-file signature, link/copy use the configured subscription bot, long identifiers wrap, and the refresh control no longer overlays mobile content. | Fresh production files proved the legacy bridge was 0 bytes while the always-on file held 37 privacy-safe rows. `/api/intel?no_cache=1` returned `200`, current board freshness and an honestly stale subscriber-attention source. Arbitrage, Charts and Funding links, Pause/Resume and the external bot link were exercised without sending a message. Desktop/mobile light/dark screenshots are clean; widths are exactly `1440/1440` and `375/375`; the final mobile pill/card intersection is false. Console has zero errors/warnings. Local/host/app/collector `server.py` and `intel.py` hashes match; full suite `1,463 passed`; Ruff `0` new (`532` known). | The real subscriber-attention feed is about 81 hours stale, so populated hot/queue/reality panels were not fabricated. The next genuine subscriber-bot lookup will invalidate the cache immediately and activate the page. No Telegram message was sent. |
| Triage `/triage` | Dark mode rendered pale text on hard-coded white lane/card surfaces. The document nested a second `<main>` inside the shell landmark. “Local Telegram” and “No local source detail” misdescribed the always-on privacy-safe subscriber feed. The fixed refresh/Pause pill covered the desktop Stale Route panel; that overlay risk existed on every long auto-refresh page. | Triage lanes, cards, badges and metrics now use shared terminal theme tokens; the internal column is a `<div>`; source/API copy explicitly identifies the anonymous subscriber-bot attention feed. The global refresh control now lives in the header's normal layout with a body fallback, remaining available without covering data at any viewport. | Production API and DOM reconcile exactly: fresh board, one Telegram source gap at about 81.2h, all six other buckets zero, and zero dashes/refreshing/unavailable/loading text. Intel and Board links, theme, Pause/Resume and API were exercised. Desktop/mobile light/dark screenshots are clean; document widths are `1440/1440`, `375/375`, and an extra `320/320`; one main landmark; refresh/main intersection false. Dark contrast is 6.93:1 or better and light descriptive contrast is at least 4.59:1. Console and request log are clean. Local/host/app/collector server hashes match; full suite `1,465 passed`; Ruff `0` new (`532` known). | Genuine populated route/funding/community cards could not be exercised in production because the subscriber-attention feed is actually stale; no Telegram event was fabricated. Their shared renderer remains covered by focused fixture tests. |
| Signals `/signals` | Dark mode rendered pale text over hard-coded near-white lane surfaces. The page ignored the always-on webhook's real `chat_signal` bucket, described the source as a local listener, and offered 1h/12h/48h controls even though the default window was 6h and no current state was announced. | Added a Subscriber Lookups lane for `recent_events.chat`, safe token links and a non-numeric event-kind fallback; moved every lane/event surface to shared theme tokens; corrected privacy/source copy and API mode; added 1h/6h/12h/48h controls with `aria-current` on the selected window. | Production desktop/mobile light/dark screenshots are clean at exact `1440/1440` and `375/375` document/main widths. All four windows, theme, Pause/Resume and the API were exercised; the selected URL/control state stayed exact. `/api/signals?no_cache=1` returned `200`, mode `read_only_subscriber_intel`, fresh board, honestly stale subscriber attention and zero event buckets matching the DOM. There were zero dashes/refreshing/unavailable/loading strings, no refresh/content intersection, dark contrast 7.11:1 or better, light descriptive contrast 4.68:1, and a clean console/request log. Fixture tests cover a populated lookup and URL-escaped token link. Local/host/app/collector hashes match; full suite `1,467 passed`; Ruff `0` new (`532` known). | The genuine source is about 81.7h stale, so a populated production lookup card was not fabricated. Legacy alert/close/community/question sources remain honestly empty; only the actual subscriber lookup lane has current always-on infrastructure. |
| Community `/community` | Dark mode rendered pale text over hard-coded near-white scoreboard, ledger and discussion surfaces; the shell nested a second `<main>` landmark, still claimed a retired local topic-brief source, and had no way to select the API's evidence window. A populated-fixture regression then exposed reported PnL rounded to whole units. | Community surfaces now use shared theme tokens, the inner landmark is a `<div>`, source copy names privacy-safe subscriber/source evidence accurately, and 1h/6h/12h/48h controls expose the selected window. Reported PnL preserves two decimal places. | Production desktop/mobile light/dark screenshots fit exact `1440/1440` and `375/375` widths with one main landmark and no refresh overlap. All four windows, theme, Pause/Resume, Intel, Signal tape and Playbook links were exercised. `/api/community?no_cache=1` returned `200`, mode `read_only_subscriber_intel`, a fresh board, stale subscriber attention and zero genuine call/result/question rows matching the DOM; zero dashes/refreshing/unavailable/loading/local strings, clean console/network, dark contrast at least 7.11:1 and light muted contrast 4.68:1. A populated fixture covers scoreboard, ledger, discussion, calls, results, questions, brief, safe token links and exact PnL precision. Full suite `1,470 passed`; Ruff `0` new (`532` known); production formatter and host/app/collector source hash match. | The real subscriber source is about 82.1h stale, so populated production performance/call cards were not fabricated. Source-supplied outcomes remain separate from subscriber lookups, which are never promoted into performance claims. |
| Learn `/learn` | The visually clean page still taught stale or incomplete product truth: it called the always-on collector a `local source`, collapsed funding carry and spread convergence into one vague idea, reduced Spot-Spot to “needs transfer rails,” and described the whole member product as read-only despite real journal/alert/account/billing mutations. Seven cards also left the final safety boundary stranded at half width. | Rewrote the page around exact route direction, separate funding and spread opportunities, volatility/margin limits, canonical-source freshness, DEX identity/provider failure semantics, and a scoped read-only trading boundary. Added Methodology, live Status and Guide actions; the odd final card spans the full desktop grid. | Failing-test-first coverage in `tests/test_learn_page.py`; full suite `1,478 passed`; Ruff ratchet `0` new (`532` known); complete 51-file production verifier passes. Production desktop/mobile light/dark screenshots fit `1440/1440` and `375/375`; seven cards, one main landmark, all mobile cards inside the viewport, dark body contrast `16.37:1`, light `15.72:1`, zero dashes/refreshing/loading/collecting states, and the two `unavailable` words are explanatory prose rather than withheld values. Theme and all three Learn actions were exercised; `/methodology`, `/status`, `/guide`, `/learn` and versioned CSS returned `200`; console/request log clean. | No Learn defect remains. Exercising the Status link exposed a separate unclosed lead: `/status` says all monitored systems are operational while the known DEX provider is rejected; trace and resolve that only during the dedicated Status audit. |
| Proof `/proof` | The public headline described the 7 August archive as the current audit. Its modeled example treated matched opening basis as immediately earned PnL, and light-theme caveat/archive text measured only 4.34:1/4.42:1. | Added a dated current checkpoint and explicit incomplete-audit caveat; moved the 7 August evidence under a native collapsed archive; replaced opening-edge PnL with `Basis convergence captured`; kept the canonical `/api/health` link; moved the two low-contrast labels to the terminal text token. | Failing-test-first copy/structure/contrast coverage; full suite `1,478 passed`; Ruff `0` new (`532` known); 51/51 host/app/collector verifier clean. Production desktop/mobile light/dark screenshots fit `1440/1440` and `375/375` with zero overflow or withheld/loading states. Final light contrast is at least 4.68:1 and dark at least 7.11:1; archive open/close, Health, Methodology and Pricing links all work and return 200; console/request log clean. Production marker `756cbc0`; server SHA-256 `43a1dd2b1384505d47da3380e0801503c6a50103528655a25209c7f75871b417`. | The current checkpoint deliberately remains frozen while the site-wide audit is incomplete. `/status` accuracy is still a separate dedicated-page lead and is not used as Proof's health source. |
| Executor boundary `/executor` | “Never loaded here” falsely included exchange credentials and private balances/positions even though Portfolio supports opt-in private-ledger connections and production private accounting is running. The attestation hard-coded `exchange_credentials_loaded=false`, so a mistaken web-process private-key mount would still look safe. | Reframed the absent boundary around execution powers rather than all private data. The API now reports browser-sealed intake, web-process decryption, separate-worker requirement and forbidden execution permissions. The page explains the distinct read-only accounting path, and a web decryption key produces `web_secret_boundary_violation` with explicit unsafe copy. | Failing tests covered ordinary same/separate origins, configured encrypted intake and the violation path. Targeted boundary/private-accounting suite `10 passed`; full suite `1,478 passed`; Ruff `0` new (`532` known); 51/51 source verifier clean. Fresh process checks prove web `seal=true/decrypt=false`, accounting worker `seal=false/decrypt=true`; all three services healthy. Production API returns zero order capabilities, no handoff, decryption false and verdict `separate_executor_required`. Desktop/mobile light/dark screenshots fit `1440/1440` and `375/375`, one main, rightmost content 351px on mobile, zero withheld/loading states, light contrast at least 4.68:1, dark at least 7.11:1. API, Methodology and Markets links returned 200; console/request log clean. Marker `3b63e56`; server SHA-256 `b7440d5816739ba6e1402e3d8b6e320c2e4f90d7914449f98e7bf968b717b47d`. | No separate order executor or handoff is configured, which is the intended safe state. Building or authorizing one remains outside this website audit; the read-only accounting worker's root filesystem remains hardened read-only. |
| Status `/status` | The page claimed every monitored system was operational while the current OKX DEX source was provider-rejected. It omitted DEX quote health, funding-history coverage, subscription lifecycle and read-only accounting entirely. A configured Telegram bot was also enough to report operational even when the private community destination was absent, and the first draft mislabeled every partial DEX source as access-blocked. | The public API and page now report CEX and DEX separately, include funding history, subscription access and isolated read-only accounting, require both bot and community configuration for Telegram readiness, distinguish explicit access/region/auth blocks from transient DEX degradation, and use an honest component-specific degraded headline. Added machine-readable Status and Release Audit actions. | Three failing-test-first regressions cover the complete customer-service set, transient DEX diagnosis and non-data degradation wording. Full suite `1,481 passed`; Ruff ratchet `0` new (`532` known); 51/51 persisted-host/app/collector verifier clean. Production shows CEX, funding, checkout, Telegram, alerts, subscription lifecycle, read-only accounting, recovery and website operational, with only `DEX quotes` degraded and the exact OKX access-block detail. Desktop/mobile light/dark screenshots fit `1440/1440` and `375/375`; ten cards, one main, no overflow/unknown/setup state, light contrast at least 4.68:1 and dark at least 7.11:1. Both footer destinations returned 200; console and request log are clean. Marker `0081631`; server SHA-256 `9b28eaabc06872f6ec3d31de9408c6d68a86b89ad67413c068e8d04c0f1358d3`. | The external OKX DEX entitlement/region/API failure remains real; the page now discloses it and does not weaken route gates. Repair the provider separately, then require sustained verified DEX cycles before changing the component to operational. |
| Telegram `/telegram` | A cold query snapshot made public `/top` say no fresh routes matched, confusing missing startup data with market evidence. The landing page called an immutable snapshot `live` and `now` without its age, and always said the Research Pro forum was already connected even when runtime state said otherwise. Upstream token and venue labels also entered Telegram HTML unescaped. | `/top` now fails honestly to a snapshot-warming answer, and a ready digest identifies itself as the latest completed snapshot with age, token count and route count. The page heading/copy follows snapshot and community runtime state. Telegram HTML labels and query links are escaped; malformed counts fail safely. No synchronous board rebuild, stale serving or weakened route gate was added. | Failing-test-first coverage plus 180 focused Telegram/query tests; full suite `1,486 passed`; Ruff `0` new (`532` known); final 51/51 host/app/collector verifier clean. A synthetic production webhook POST returned `latest completed research snapshot`, a real age and `500 tokens · 2,255 routes` without being delivered to Telegram. Isolated exact production parsing returned 32 GUA spread routes, 49 SIREN routes, 46 ESPORTS routes, 54 BTW funding routes and six GUA transfer venues, with no warming/no-route response and attention recording disabled. Both page actions returned 200; the external action resolved to the correct bot. Desktop/mobile light/dark fit `1440/1440` and `375/375`, one main, rightmost mobile content 351px, zero withheld/loading strings, light contrast at least 4.68:1, dark at least 7.11:1, clean console and no failed HTTP request. Marker `8c07197`; server SHA-256 `f6ccf393f622fd056847fd482ea26a2abdfab8bf90f4a572e0904b7214124aef`; `telegram_bot.py` SHA-256 `4a597b5dda93aaac4fbfdbe73b5dd5184323b7ee30e1bb482414f33b0f8c87c7`. | The dedicated public broadcast feed remains intentionally optional and unconfigured. `/top` continues to use the last complete immutable snapshot so it stays inside Telegram's webhook deadline; its age is now explicit rather than falsely described as current. No group message, subscriber lookup event, trade or transfer was created. |
| Markets / Arbitrage landmark follow-up | The earlier “fully audited” claim missed a nested `<main>`: the shared shell supplied one landmark while the layout-only `.market-main` supplied a second on both `/markets` and `/arbitrage`. | Replaced only the inner layout element with a `<div class="market-main">`, preserving its CSS and all market behavior. | Wrote and watched the route-level landmark regression fail at 2 versus 1, then pass. Full suite `1,487 passed`; Ruff `0` new (`532` known); 51/51 production source verifier clean. Production `/markets` and `/arbitrage` both return 200 with one main, zero nested mains, `.market-main` still present as a DIV, exact 1440px fit and a clean console. Marker `c80e774`. | None. This correction is recorded explicitly because it contradicted the earlier completion claim. |
| Guide `/guide` | The page made several dangerous research claims: two legs were described as automatically direction-neutral, futures/spot as always convergent, 1x as effectively removing liquidation/borrowing risk, equal token counts as sufficient despite contract multipliers, DEX spot selling without inventory, large gaps as proved real, and the matched-depth probe as an unspecified small size. | Rewrote the guide around matched underlying exposure, contract multipliers, independent collateral and reserve on both venues, liquidation risk even at 1x, separate funding and convergence theses, non-guaranteed convergence, inventory requirements for DEX/spot selling, the usual long-DEX-spot/short-futures direction, exact identity, and the exact `$500 matched VWAP` evidence boundary. | Two safety tests were written and observed failing before the content change; four focused Guide/public tests pass. Full suite `1,489 passed`; Ruff `0` new (`532` known); adversarial review found and removed the final “these are real” overclaim; 51/51 production source verification is clean. Signed-in and signed-out production pass desktop/mobile light/dark screenshots, one main, exact 1440/375px fit, zero dash/refreshing/unavailable/loading strings, clean console/network, every one of nine section jumps and ten product destinations, and contrast of at least 4.68:1 light / 7.11:1 dark. Marker `19e1a47`; server SHA-256 `aea20b415cdaeeb80985cb1232cadd8c9e522b3b148334fef975e606ff3ad9d6`. | No personalized reserve number is promised. A credible reserve requires exact account leverage, liquidation distance, adverse-basis history, leg volatility and fee/funding shocks; the Guide now explains the method without fabricating a prediction. |
| Methodology `/methodology` | The primary spread formula contradicted the live calculation. The page divided by midpoint, while the route engine and displayed matched edge divide by the buy VWAP. A named ZIL HTX Spot → Gate Futures row therefore displayed `18.456817%` while the documented formula implied `16.897451%`. The price section also left the standard probe size implicit and did not distinguish unresolved matched depth from a separately labelled top-book reference. | Replaced the formula with `(sell VWAP ÷ buy VWAP − 1) × 100`, tied the prose to the server's `$500` probe constant, and stated the exact underlying-exposure, directional-depth, contract-multiplier and unresolved/top-book boundaries. No market, membership, billing or scoring behavior changed. | The named regression was written and observed failing before the copy fix. Focused Methodology/public tests `40 passed`; full suite `1,490 passed`; Ruff `0` new (`532` known); adversarial review found no new trust-boundary or evidence overclaim. Fresh production ZIL HTX Spot → Gate Futures reconciled `0.002327 → 0.002713` to `16.587881392350678%`; the live formula differed by only floating-point noise while midpoint would say `15.317460%`. All seven jumps and four link controls were exercised in public and authenticated flows; desktop/mobile light/dark screenshots fit `1440/1440` and `375/375`, one main, no nested main, no withheld/loading state, light contrast at least 4.68:1, dark at least 7.11:1, and clean console/200 request log. Source sync is 51/51. Marker `18f9f2c4bcc7c8050eb5905f6e43fbd7d9e680d0`; server SHA-256 `076c5672c52bac75e567691529f5956639b0f4937c4bfbb8d12705b592b3f97f`. | No Methodology defect remains. The page intentionally explains the fail-closed future ML gate rather than presenting current readiness counts; current evidence belongs in the audit/status surfaces. |
| Pricing `/pricing` | The page overclaimed `Every spread, live`, fixed coverage at `22 exchanges plus OKX DEX`, implied every lane/price/identity was always exact, advertised unsupported priority support, hid Scanner's 90/365-day prepaid terms, offered an impossible mid-term Scanner switch to an active Research Pro member, trusted a bare `?referred=1` query as proof of a discount, and rendered light muted copy at only `4.42:1`. | Replaced the claims with source-dependent, evidence-labelled coverage; separated current-book freshness, `$500` depth and identity status; removed priority support; rendered exact 30/90/365-day USDC/USDT-on-Arbitrum terms for both tiers from the billing configuration; routed active members to Subscription after their current term; required a valid first-party referral cookie backed by an affiliate record; and added a Pricing-scoped contrast-safe muted token. | Failing-test-first regressions cover claims, both term grids, active-plan actions, valid/invalid referral state and contrast. Full suite `1,494 passed`; Ruff `0` new (`532` known); 51/51 source sync clean. Fresh billing health is checkout-ready on Arbitrum One with six confirmations, USDC/USDT and exact Scanner `49/135/490` and Research Pro `149/375/1365` prepaid prices; no recurring claim is made. Public and authenticated controls/destinations pass, the active Research Pro account has no `Choose Scanner` action, and a direct forged referral query shows no banner. Final desktop/mobile light/dark captures fit exact `1440/1440` and `375/375`, one main/no nested main, zero console errors, Pricing/CSS `200`, light muted contrast `5.26:1` and dark muted contrast `7.44:1`. Marker `f01fece990cf7f6bd5dd20572f3e3597500d53fd`; server SHA-256 `6f1b43a78847e57dedfff2f8cf171e7bf3f4b38268dadc5fd011eef6e68ed426`. | No Pricing defect remains. The positive referral-cookie path is exercised by an exact local HTTP integration test; the public dynamic `/r/*` route still receives its own dedicated route audit later, without creating a production affiliate click now. |
| Subscription `/subscription` | An active Research Pro member could forge `?tier=scanner` and make the page call Scanner selected even though the billing boundary rejects mid-term tier changes. The page exposed both tiers' live crypto buttons, dead purchase controls to administrators/signed-out users, crypto alongside a managed recurring account, misleading auto-renewal/next-payment copy after scheduled cancellation, and vague “contact support” copy despite supported same-tier renewal. Clipboard feedback claimed success before the write resolved, invoice polling hid repeated failures forever, the consent error named policy links that are intentionally absent, and light muted copy measured only `4.42:1`. | Current/billing tier now locks the displayed plan and other tiers are visibly unavailable. Direct prepaid members see only their current/selected tier's three terms and accurate same-tier extension copy; managed billing gets only its portal and truthful cancellation state; administrators and signed-out renders have no customer purchase controls. Clipboard success is awaited with an explicit failure state, three consecutive status failures stop polling and surface a do-not-repay warning, consent copy matches the visible acknowledgement, and a Subscription-scoped muted token restores contrast. | Ten failing-first truth regressions plus the related billing/membership suites pass (`91 passed`); final full suite `1,506 passed`, Ruff `0` new (`532` known), and 51/51 production source sync is clean. Public production redirects to `/login?next=%2Fsubscription`. The active Research Pro account resisted a forged Scanner query, exposed exactly three Research Pro buttons, one main and no overflow at 375px. A disposable inactive member selected both tiers and exposed only three matching buttons each. Production created one Research Pro and one Scanner 30-day invoice through the real UI, rendered USDC/USDT, exact QR/wallet/token switching, polling and truthful Copy feedback; no crypto was sent. Exact invoice/consent rows and the inactive user/session were then deleted and independently counted at zero. Desktop/mobile light/dark captures are clean; light contrast is `5.26:1`/`5.56:1`, dark `7.44:1`/`7.11:1`. Marker `69cae15f344db4d885b8c479d7d2ab64201a71e5`; server SHA-256 `0b309847fa860a0ca548eb90ffdef08d9a29eda8b8fe58744adf9bf6495653cd`. | No Subscription defect remains. The retained authenticated audit account stays disposable until the complete site-wide audit is finished; no payment, subscription extension or Telegram action occurred. |
| Encrypted production backup | The six-hour timer had failed repeatedly. `ProtectHome=true` hid rclone's only config under `/root`, and after exposing a root-only copy the consistency-safe SQLite backup still could not open the WAL-mode accounts database because the runtime tree was mounted read-only inside the service. The traceback also failed to identify which database was blocked. | The hardened unit now uses a `0600 root:root` rclone config beside the existing backup secrets, keeps root home hidden, permits writes only to the runtime path so SQLite may create its transient `-shm`, keeps application code read-only, and reports the exact relative database when consistency copying fails. | Seven focused backup regressions and the final `1,506`-test suite pass. Service run `17:02:33–17:19:47 UTC` completed successfully despite retried Google shared-client rate limits: snapshot `ceea08b8`, 88 files, `3.727 GiB` processed, `934.406 MiB` stored. The snapshot contains the `3,578,011,648`-byte market-history DB and a `417,792`-byte accounts DB. Retention kept two snapshots; `restic check --read-data-subset=1/20` found no errors. A separate `restic dump` restored the new accounts DB into a temporary directory and proved `PRAGMA quick_check=ok`, 36 tables, 12 users and zero temporary inactive-audit users. The timer is enabled/active; unit/script hashes match the committed files. | No backup incident remains. Google Drive's shared rclone client did hit and recover from per-minute quota errors; a private free Google OAuth client remains a resilience/performance improvement if future runs repeatedly approach the next six-hour window. |
| Future ML readiness | Data volume, route coverage, class balance, leakage, and purged time split pass; exact-cost completeness and time span do not. | No model trained or activated; shadow deterministic fallback preserved. | Fresh production worker through `/app/.venv/bin/python`: `28,252` v5 observations, `18,916/5,000` labeled 24h outcomes, `1,947/100` labeled routes, `7.29/30` labeled days, `0.0/0.8` exact-cost completeness, funding minority `0.1133`, spread minority `0.2756`, leakage clean, valid 24h-embargoed split (train `11,204`, calibration `890`, test `1,151`, purged `5,671`), no candidate and activation false. Worker returned `status=ok` with `5,285` tokens, `1,615` live and zero cooled; this pass captured 41 observations and 11 route-history rows, with no exact-cost, transfer-evidence or matured-label row. The v5 observation store spans `8.79` days; only the `7.29`-day labeled span may satisfy activation. Database quick-check, foreign-key, duplicate route/hour/method, duplicate outcome, nonpositive-sample, future-observation/outcome, target-window, horizon, feature-JSON and orphan-outcome checks pass; all `5,832` v4 rows remain excluded. | Preserve collection for at least 30 labeled days and collect opt-in exact lifecycle-cost evidence to reach 80%; candidate evaluation remains correctly absent until data readiness is valid. |

| Partner `/partner` | Admin creation succeeded in the database but its async handler dereferenced `event.currentTarget` after `await`, so the UI threw and did not refresh; partner Copy Link failed for the same reason. Zero-payable payout creation stayed enabled and surfaced a raw internal error. Draft batches had no cancellation/recovery path, payable and held commission were conflated, payout values used dollar labels despite USDT-only settlement, direct admin referral links could inflate click counts, invite failure could strand an orphan account, the slug pattern caused a Chromium console error, and the 820px ledgers expanded the whole mobile document to 890px. | Captured stable form/button references before async work; added explicit response/error handling, human empty states, copy-only referral/setup handoff, exact held-versus-payable totals, USDT labels, recurring-terms wording, commission void control, and recoverable draft cancellation that atomically restores commissions. Failed partner/token creation now deletes only the exact untouched invite. The browser-valid slug pattern and min-width containment keep wide ledgers inside their own scroller. | Production exercised admin, partner and ordinary-member states; UI partner creation/reset and copy succeeded; active/paused referral behavior was correct; a synthetic no-transfer lifecycle covered settled commission, payable action, draft creation, cancellation/restoration, rebuild, paid reference, second commission and void. Partner desktop/mobile light/dark, populated and empty states have one main, zero console errors, no page overflow (`1440/1440`, `375/375`), and internal table scrolling only. All four disposable users, sessions, affiliate/invoice rows, Keychain credentials and browser state were removed; affiliate tables returned to zero, `PRAGMA quick_check=ok`, and only Alex and Anatolij remain admins. Full suite `1,515 passed`; Ruff `0` new (`532` known); 51/51 host/app/collector sync clean at marker `7947c3cd6562c5195033011fc12ff3a4d00ad00d`. No crypto transfer or Telegram message occurred. | No Partner defect remains. Production has no configured influencer yet, so the verified populated lifecycle was deliberately synthetic and fully removed. `/free` was the next checkpoint and is now complete; continue at auth/legal routes. |
| Free preview `/free` | A cold current-generation render could remain a permanent `0 tokens / 0 routes` shell: SSE could report a live board but could not create the missing rows, and the page had no structural recovery. Spread and funding evidence could be owned by different routes but shared one stream identity; instantaneous funding could overwrite a settled-24h value without changing its label. Funding teasers could rank one route/value but display another route field, while the copy overclaimed open depth, future settlement and fixed OKX DEX coverage. | Added a silent in-place structural refresh every 5 seconds while warming and every 300 seconds when live, with explicit structure-refresh dispatch, old-stream closure and new-stream connection. The unlock dialog is delegated and survives root replacement. Full groups carry separate exact spread and funding route keys; the stream allow-list covers only identities already disclosed, while locked teaser routes remain opaque. Settled funding is not a live-update target and retains its owning route; current projections stay explicitly labelled. Teaser economics and source-dependent DEX/depth copy now match the displayed evidence. | Public production recovered in place from `71 tokens / 216 routes` to `963 / 11,583` with the same `performance.timeOrigin`; an authenticated Research Pro session recovered from `0 / 0` to `951 / 10,389`. Both reached four groups and 12 locked rows with 300-second live cadence. Click and keyboard unlock paths worked after replacement. Separate DOM route keys, settled/current funding hooks and exact disclosure boundaries passed. Desktop/mobile light/dark fit exactly (`1440/1440`, `375/375`), one main, no page overflow, light muted contrast `4.6803:1`, dark `7.1141:1`, all visible public destinations loaded, and console/network checks were clean. Full suite `1,525 passed`; Ruff `0` new (`532` known); all 51 modules match persisted host/app/collector at marker `6e203ad`. No trade, payment or Telegram message occurred. | No Free defect remains. Token/route counts are current snapshots and may differ across sessions. Continue the exhaustive audit with auth/legal routes; the overall site audit is still open. |
| Forgot password `/forgot-password` | The page was dark-only, the no-JavaScript form defaulted to GET and could put an email in the URL, known accounts synchronously waited on the provider and leaked account existence through timing, and configuration presence was labelled operational without delivery evidence. Provider acceptance IDs were discarded, errors were swallowed, the stated queue bound was actually unbounded, queued work could remain pending indefinitely, old SMTP evidence could prove a current Resend setup, Caddy made all visitors share one rate-limit identity, queue saturation was silent, and a denied `localStorage` write could break the theme toggle. | Added light/dark parity and safe theme persistence; explicit POST and generic 303 no-JavaScript confirmation; account-agnostic asynchronous handling; a truly bounded worker-plus-pending queue with generic busy state; trusted-proxy client-IP resolution; privacy-safe delivery lifecycle evidence with provider, state and timestamps but no recipient, token or public provider ID; provider-mismatch, stale, pending, stalled and error status states; and exact wording that distinguishes provider acceptance from inbox delivery. | Targeted password-recovery tests `27 passed`, wider auth/status tests `122 passed`, full suite `1,536 passed`; Ruff has `0` new findings (`530` known), and `git diff --check` passed. All 51 modules match persisted host/app/collector at marker `61bb68584bd95b44fc82ff0084589dc505a7a2f2`. A real authenticated production reset returned generic `202` in `275 ms`; Resend accepted it in `0.71 s`, a provider ID was present, pending jobs were zero and the production accounts DB passed `PRAGMA quick_check`. `/api/status` then reported the accepted evidence with its timestamp. Public/authenticated desktop and public mobile light/dark fit exactly (`1440/1440`, `375/375`), one main, clean console, contrast from `4.58:1` to `17.64:1`, unknown-account generic UI, working back navigation, and no-JavaScript `303` to `/forgot-password?requested=1` with no email in the location all pass. | Provider acceptance is proven; inbox delivery is deliberately not claimed. The queue remains in-process, so a hard kill before a worker persists its row can lose that queued attempt; a durable external queue is not justified at current scale. Consolidate the duplicated auth shell while auditing the adjacent auth pages. Continue at `/login`; the overall audit remains open. |
| Login `/login` | The page was dark-only and had no theme control. Its form had neither `action` nor `method`, so no-JavaScript submission placed both email and password in the URL. The user-controlled `next` path was inserted raw inside an inline script; a harmless production payload proved executable script breakout. Unknown accounts skipped the password hash, input lengths were unbounded, HTML login had no login-CSRF origin boundary, direct sign-in landed on the public root, and an already-authenticated visitor still saw the sign-in form. | Added light/dark parity with safe persistence and a no-script-hidden theme control; explicit POST progressive enhancement; generic 303 failure state with no credentials in URL; same-origin enforcement for HTML form login; one server-side local-path sanitizer that rejects schemes, authority-like slashes/backslashes, controls and HTML delimiters; server-owned destinations for JSON and form login; unknown-account dummy scrypt work; 254/1,024-character bounds; password clearing/refocus and busy state; direct `/account` landing; and authenticated redirect without re-presenting credentials. | The nine new login regressions fail on the old implementation and pass; wider auth coverage is `70 passed`, final full suite `1,545 passed`, Ruff has `0` new (`530` known), and `git diff --check` passes. All 51 modules match persisted host/app/collector at marker `65d95c3e3b272d53b42b1f426209f9cdca81f9dd`; app, collector and accounting worker are healthy. Production desktop/mobile light/dark fit exactly (`1440/1440`, `375/375`), one main and clean normal console; measured contrast is `4.58:1–17.64:1`. The former script-break payload now produces no marker/error and falls back to `/account`; JavaScript failure clears/refocuses the password, while no-JavaScript failure redirects only to `/login?next=%2Faccount&error=invalid`. A disposable inactive member proved successful JavaScript and no-JavaScript login plus authenticated `/profile` redirect; the user and all three sessions were deleted and `PRAGMA quick_check=ok`. | No Login defect remains. Chrome logs the expected failed-resource line for the intentionally exercised 401, while normal navigation is clean. The no-JavaScript success destination exposed a separate `/account` mobile overflow (`423px` document at a `375px` viewport); keep it open for the already scheduled Account audit. Continue at `/register`; the site-wide audit remains open. |
| Registration `/register` | The page was dark-only; authenticated members still saw the form; its no-JavaScript default was GET and would place the display name, email and password in the URL. HTML registration had no same-origin boundary, field sizes were unbounded, duplicates explicitly enumerated an existing account and unexpected exceptions could expose raw internal detail. User creation, referral attachment and login used separate commits, so an affiliate/session failure could strand an account or attribution. Referral click tokens were replayable, and the session IP hash used a synthetic `register:` prefix rather than the real client IP. | Added light/dark parity with safe persistence and no-script-hidden theme control; explicit POST progressive enhancement; same-origin HTML registration; 100/254/1,024-character bounds; precise safe validation with generic duplicate/unexpected errors; correct active/inactive authenticated redirects; real client-IP privacy hashing; and one atomic `BEGIN IMMEDIATE` transaction covering user, single-use referral attribution and session. Password hashing occurs before the write lock and downstream failure rolls everything back. | Seven failing-first registration/security regressions pass, wider auth/account/affiliate coverage is `114 passed`, final full suite `1,552 passed`, Ruff has `0` new (`530` known), compile/diff checks pass and all 51 persisted-host/app/collector modules match marker `08f626e39c427b520b6a75bfd25d456be369fa52`. Production desktop/mobile light/dark fit exact 1440/375px with one main, clean console/network and minimum measured contrast `4.583:1`; theme toggle and reload persistence work. Empty/name-email/short-password browser validation focuses the exact invalid field without a POST; Login, Reset and Membership links return 200. Cross-origin form is rejected 403 before mutation, uppercase JSON media type is parsed, overlong password is rejected, and duplicate handling is generic. Exact JavaScript and no-JavaScript members both landed on `/subscription` with no credentials/query in the URL; inactive `/register` redirected to Subscription and the exact active member redirected to Account. The two users, all three sessions and any attribution were exact-match deleted; counts returned to zero, quick-check and foreign-key check pass. During deployment a transfer-boundary mistake briefly installed a zero-byte `server.py`; it was re-uploaded, byte/hash verified, services recovered healthy and 51/51 source sync was reproved. | No Registration defect remains. The older auth pages still duplicate visual-shell code; Set Password is complete and the broader refactor is deferred without a demonstrated defect. The site-wide audit remains open; continue at `/terms`. |
| Set password `/set-password` | The reset route embedded the full member shell around a nested main, clipped navigation on mobile, had no explicit progressive form action/method, no HTML same-origin boundary, unbounded fields, raw error surfaces and a user-controlled `next` value in executable script. Token consumption could race because lookup, password update, session deletion and token claim were not one locked transaction. Caddy's referrer policy also allowed reset URLs to leak. The first deployed origin boundary then rejected real script-disabled Chromium, which sends `Origin: null` with browser-controlled same-origin Fetch Metadata. | Replaced the route with a standalone one-main auth document and light/dark safe persistence. Added explicit POST progressive enhancement, local-path-only `next`, 1,024-character bounds, generic errors, a separate reset limiter and no-referrer policy. Password hashing happens before `BEGIN IMMEDIATE`; token lookup/conditional claim, password update and all-session revocation commit atomically or roll back. Normal matching Origin is accepted; absent/null Origin is accepted only with exact `Sec-Fetch-Site: same-origin` plus `Sec-Fetch-Mode: navigate`, while cross-site/null and legacy unproven requests fail closed. | Five failing-first reset/security regressions and `48` targeted auth/reset tests pass; final full suite `1,557 passed`, Ruff `0` new (`530` known), and 51/51 local/persisted-host/app/collector source sync passes at marker `3234edfa1e29eb14498ef7db9303459cbd9992d8`. A complete production JavaScript and no-JavaScript lifecycle covered valid, mismatch, short, cross-origin, used and expired tokens; no password entered the URL, safe `next` landed on Partner, prior sessions were revoked and old passwords failed. Desktop/mobile light/dark valid/expired screenshots fit exact 1440/375px with one main, no site nav, zero normal console/network failures and contrast `4.58:1–17.64:1`. The exact disposable user, its three tokens and temporary Keychain items were removed; counts are zero, `PRAGMA quick_check=ok` and foreign-key violations are zero. | No Set Password blocker remains. Site-wide CSP still permits inline script/style and the older auth pages retain duplicated visual-shell code; both are broader refactors with no demonstrated Set Password defect and remain deferred. The site-wide audit remains open; continue at `/terms`. |
| Terms `/terms` | The legal body nested a second `<main>` inside the shared shell; two light-theme text/action combinations were below AA; contact email and Telegram were inert text; support URL configuration accepted ambiguous values. The shared authenticated shell also redirected as soon as logout headers arrived, aborting its JSON body, never checked response success, had no failure feedback or timeout, and hid sign-out entirely on mobile. | Replaced the inner landmark with `.legal-sections`; scoped legal/Membership colors to AA-safe tokens; rendered validated `mailto:` and HTTPS Telegram anchors; rejected ambiguous support URLs. Logout now consumes and verifies `{ok:true}`, keeps CSRF, times out after 15 seconds, restores a retryable button and visible bounded status on failure, and remains available at 375px. | Nine legal regressions, 23 related tests and the final `1,566`-test suite pass; Ruff `0` new (`530` known), compile/diff checks and 51/51 persisted-host/app/collector source sync pass at marker `cc7fe31ab9f8023ed860a7cb0ab0dc94588e5144`. Public/authenticated desktop/mobile light/dark all return 200, fit exact 1440/375px, contain one main/zero nested mains and two working contacts. The earlier full matrix exercised every visible internal destination and both external support controls without sending a message. A final live 375px forced-503 flow proved the notice is visible/in bounds, the session stays authenticated and retry is enabled; the real retry reaches `/login`, returns session 401 and produces no aborted logout request. All audit-account sessions were exact-match deleted and counted at zero. | Legal routes remain directly reachable but intentionally absent from the global product nav. Site-wide inline CSP and shared-shell duplication remain broader deferred refactors. `/privacy`, `/refunds` and `/affiliate-terms` still require their own content/control production passes; resume at `/privacy`. |
| Privacy `/privacy` | The notice omitted stored Web Push endpoint/key/user-agent data, purposes and legal bases, the required/optional distinction, sharing/international processing, retention, automated-decision limits and several privacy rights. It had no safe configurable controller identity/address, its kicker was below AA in light mode, and the final production matrix exposed shared inactive navigation at only `4.424:1`. | Rewrote the notice to match actual stored account, payment, research, security, recovery and Web Push data; added purposes/legal bases, required/optional boundaries, sharing/transfers, retention, rights, portability, objection/withdrawal and significant-automated-decision limits. Controller name/address are escaped, bounded and environment-configurable. Legal actions and the shared inactive navigation use scoped AA-safe tokens. | Privacy/legal regressions and `50` related tests pass; final suite `1,569 passed`, Ruff `0` new (`530` known), compile/diff checks and 51/51 source sync pass at marker `2788bdfd4b59681a4e8eb5be0f14ad492046337f`; server SHA-256 is `eff376e4f390f9b350744ecba77420d40a7b8054a4d4882ecaba707344c72e95`. Public/authenticated desktop/mobile light/dark all return 200, fit exact 1440/375px, have one main, all 16 expected headings, two working contacts, theme persistence and every visible destination with clean console/network. Light navigation contrast is `4.745:1`, dark `7.444:1`; Privacy kicker is `4.866:1` light and `10.725:1` dark. Logout reaches Login/session 401; exact audit-session cleanup returned to zero. | The owner still must supply the actual legal data-controller name and address through `SPREADBOARD_DATA_CONTROLLER_NAME` and `SPREADBOARD_DATA_CONTROLLER_ADDRESS`; the honest production fallback remains “SpreadBoard” and must not be guessed. Jurisdiction-specific legal sufficiency requires qualified counsel. Continue at `/refunds`, then `/affiliate-terms`; site-wide inline CSP/shared-shell duplication remain broader refactors. |
| Refunds `/refunds` | The policy and checkout said immediate access merely “may affect” a statutory cancellation right instead of stating the actual requested choice. Service-fault copy omitted the repair/replacement or repeat-performance remedy before an appropriate price reduction/refund, and crypto-refund wording left the original asset and destination to operator discretion. The legal/consent version was still `2026-08-12`. | Added one shared escaped immediate-access statement used by both checkout and Refunds; it says the applicable right remains until paid digital access begins, is lost when paid access begins, and does not affect fault rights. Added clear-statement cancellation before access, repair/replacement or repeat performance, reasonable-time/no-significant-inconvenience limits, price reduction/full refund, 14-day processing where applicable, original payment asset and verified destination unless expressly agreed otherwise, no prohibited fee, and the blockchain/statutory-remedy boundary. Bumped the legal consent version to `2026-08-22`. | Two regressions were observed failing before the change; `71` related legal/membership/billing tests and the full `1,571` suite pass. Ruff has `0` new findings (`530` known), compile/diff checks pass, and all 51 modules match the persisted host/app/collector at marker `3eb4ff35ccbd71b70a7e519d48a3d536168bcc30`; server SHA-256 is `ad49ba53afbc1a32115547057477822fd6890df47fd2f3cf3f388595e0d3cbed`. Public/authenticated desktop/mobile light/dark all return 200, fit exact 1440/375px, have one main/zero nested mains, all five headings, zero withheld/loading strings, exact new remedy text, working contacts and every visible destination, theme persistence, no console/network failures, and minimum contrast `4.745:1` light / `7.444:1` dark. The real active-member Subscription page renders the same unchecked consent beside all three Research Pro crypto terms. Mobile logout returned 200, reached Login/session 401; four audit sessions were exact-match deleted, leaving zero, with SQLite quick-check `ok` and zero foreign-key violations. | Jurisdiction-specific legal sufficiency and whether a durable post-purchase contract copy must be emailed require qualified counsel and a broader transaction-mail decision; no email behavior was invented during a page-copy repair. Legal routes remain directly reachable but intentionally absent from global product navigation. Site-wide inline CSP/shared-shell duplication remain deferred. Continue at `/affiliate-terms`. |
| Affiliate terms `/affiliate-terms` | The public page covered the core percentages but omitted material rules already present in the signed agreement and actual affiliate engine: Provider/partner relationship and authority, no minimum or exclusivity by default, automatic-versus-eligible payout wording, statement and wallet responsibility, sanctions/compliance, brand and customer-data boundaries, suspension/termination, service/earnings limits and the signed documents' priority. “Enters the next payout batch” also overstated an owner-created manual batch. | Added the missing operating boundaries without changing the engine or commercial rates. The copy now defines Provider by the signed agreement, states independent-contractor/no-binding authority, no minimum sales, settled-invoice-only commission, seven-day hold followed by payout eligibility, USDT on Arbitrum One, 30-day statement disputes, wallet/network responsibility, disclosure/compliance/data limits, suspension, no earnings guarantee and signed Agreement/Deal Sheet priority. | The regression was observed failing before the fix. `40` related legal/affiliate/cabinet/subscription tests and the full `1,572` suite pass; Ruff has `0` new findings (`530` known), compile/diff checks pass, and the fresh whole-package verifier matches all 51 modules on persisted host/app/collector at marker `fa74bd4fd536a7374dfe28f9a42a54538ee9e229`; server SHA-256 is `9dc762cf2f309004d1d2e1bd30a33e1da1f0bd3c6e5d69ead5653657900ef9cf`. Public/authenticated desktop/mobile light/dark all return 200, fit exact 1440/375px, have one main/zero nested mains, all 12 headings, the exact disclosure, correct mail/Telegram contacts, every visible destination, theme persistence, zero console/network failures and minimum contrast `4.745:1` light / `7.444:1` dark. The single dash is disclosure punctuation and the single “unavailable” is truthful service-limit copy, not withheld data. Mobile logout reached Login/session 401; the exact audit account has zero sessions, SQLite quick-check is `ok` and foreign-key violations are zero. | Qualified counsel must review jurisdiction-specific terms and the owner must supply Provider legal name/address and governing-law details before the first agreement is signed. Public legal routes remain intentionally absent from global navigation. Legal copy is still duplicated across renderer, signed draft and partner pack; a single-source document refactor is deferred because it crosses product/document-generation boundaries. Continue at `/account`. |

## Named commit re-verification

- `2406456`: verified in signed-in production Funding.
- `aa288c5`: verified with two disposable production portfolio fixtures.
- `7557724`: verified with partial funding/PnL fields; discovered and fixed the contradictory note locally.
- `6029032`: verified in the first eight signed-in production ranking rows.
- `b3d7a09`: first contradicted by production drift (named ANTHROPICX on about
  `$42` turnover), then repaired and verified on 34 current cards with zero
  unknown/below-floor turnover.
- `33f5410`: first contradicted by production source drift, then repaired and
  verified with the exact BTW thin reverse-book condition in a production
  process and numeric exact-book values in the live chart pipeline.
